@using MangoTaika.Data.Entities
@model RapportActivite
@{
    ViewData["Title"] = Model.Activite.Titre;
    Layout = "_LayoutAdmin";
    var canManage = User.IsInRole("Administrateur") || User.IsInRole("Gestionnaire");
}

<div class="d-flex justify-content-between align-items-start flex-wrap gap-3 mb-4">
    <div>
        <h2 class="section-title mb-1"><i class="bi bi-journal-richtext text-or"></i> <span class="accent">@Model.Activite.Titre</span></h2>
        <p class="text-muted-custom mb-0">Rapport post-activite</p>
    </div>
    <div class="d-flex gap-2 flex-wrap">
        @if (canManage)
        {
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-bleu"><i class="bi bi-pencil"></i></a>
            @if (Model.Statut is StatutWorkflowDocument.Brouillon or StatutWorkflowDocument.AReviser)
            {
                <form asp-action="Soumettre" asp-route-id="@Model.Id" method="post" class="d-inline">@Html.AntiForgeryToken()<button type="submit" class="btn btn-mango"><i class="bi bi-send"></i> Soumettre</button></form>
            }
            @if (Model.Statut == StatutWorkflowDocument.Soumis)
            {
                <form asp-action="Valider" asp-route-id="@Model.Id" method="post" class="d-inline">@Html.AntiForgeryToken()<button type="submit" class="btn btn-mango"><i class="bi bi-check2-circle"></i> Valider</button></form>
                <form asp-action="DemanderRevision" asp-route-id="@Model.Id" method="post" class="d-inline">@Html.AntiForgeryToken()<button type="submit" class="btn btn-outline-or"><i class="bi bi-arrow-counterclockwise"></i> Revision</button></form>
            }
        }
        <a asp-action="Index" class="btn btn-outline-or"><i class="bi bi-arrow-left"></i></a>
    </div>
</div>

<div class="card-3d p-4 mb-4">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
        <div class="badge-3d badge-soumise">@Model.Statut</div>
        <small class="text-muted-custom">Rapport cree le @Model.DateCreation.ToString("dd/MM/yyyy")</small>
    </div>
    @if (!string.IsNullOrWhiteSpace(Model.CommentaireValidation))
    {
        <div class="alert alert-warning border-0">@Model.CommentaireValidation</div>
    }
    <div class="row g-4">
        <div class="col-12"><h5 class="text-or">Resume executif</h5><div class="text-muted-custom" style="white-space:pre-line;">@Model.ResumeExecutif</div></div>
        <div class="col-lg-6"><h5 class="text-or">Resultats obtenus</h5><div class="text-muted-custom" style="white-space:pre-line;">@Model.ResultatsObtenus</div></div>
        <div class="col-lg-6"><h5 class="text-or">Difficultes rencontrees</h5><div class="text-muted-custom" style="white-space:pre-line;">@Model.DifficultesRencontrees</div></div>
        <div class="col-12"><h5 class="text-or">Recommandations</h5><div class="text-muted-custom" style="white-space:pre-line;">@Model.Recommandations</div></div>
        <div class="col-12"><h5 class="text-or">Observations complementaires</h5><div class="text-muted-custom" style="white-space:pre-line;">@(string.IsNullOrWhiteSpace(Model.ObservationsComplementaires) ? "Aucune observation." : Model.ObservationsComplementaires)</div></div>
    </div>
</div>
